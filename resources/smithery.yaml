# smithery.yaml — GEOX Smithery MCP Registry Configuration
# DITEMPA BUKAN DIBERI
#
# Smithery is a marketplace for MCP servers
# Registry: https://smithery.ai
#
# P0c (2026-07-23): REGENERATED FROM CANONICAL_PUBLIC_SURFACE.json
# Source schema: geox.canonical_public_surface.v1
# Canonical tool count: 31
# Every advertised name below exists in the live GEOX surface.
# Internal operations live as MODES on canonical tools, not as new tools.

name: geox-earth-witness
version: "1.0.0"
display_name: "GEOX — Geological Intelligence Coprocessor"
description: |
  GEOX is a governed, agentic geological intelligence coprocessor for arifOS.
  Provides physically-grounded geological prospect evaluation using Large Earth Models,
  seismic VLM interpretation, basin simulation, and geospatial verification.
  
  Key capabilities:
  - Prospect evaluation with constitutional floor enforcement
  - Structural validation (fault mechanics, shortening calculations)
  - Geological memory RAG over past evaluations
  - Tri-app architecture: Map + Cross Section + Seismic Section
  
  Verdict outcomes: SEAL (high confidence), PARTIAL (supported with caveats),
  SABAR (wait/gather more), VOID (contradictions detected)

category: science
subcategory: geology

# MCP protocol
mcp_version: "2025-11-25"
transport: http
command: python
args: ["server.py"]

# Installation
install:
  method: pip
  package: arifos-geox
  source: https://github.com/ariffazil/geox
  python_version: ">=3.11"

# Canonical Public Surface — 31 tools
# Every tool name below is in the live GEOX canonical manifest.
# Modes (subscripts) live as enum values on each tool's input schema,
# not as separate public tools.
tools:
  - id: geox_well_ingest
    name: geox_well_ingest
    description: "Load well log data from LAS, SEG-Y, DST, deviation, or tops files. Auto-detects format."
    domain: "earth.well"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_petrophysics
    name: geox_petrophysics
    description: "Unified petrophysics: Vsh, porosity, Sw, permeability, net pay, LEM inference, QC. Modes: generate, verify, lem_inference, stoip_feed, qc."
    domain: "earth.petrophysics"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_seismic_ingest
    name: geox_seismic_ingest
    description: "SEG-Y I/O, header inspection, export."
    domain: "earth.seismic"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_seismic_compute
    name: geox_seismic_compute
    description: "Seismic computation: forward model, well tie, attributes, wavelet extraction, inversion."
    domain: "earth.seismic"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_seismic_interpret
    name: geox_seismic_interpret
    description: "Unified seismic interpretation: horizon/fault picking, RSI pipeline, vision, contrast detection. Modes: horizon_contrast, fault_sticks, volume_frame, blend, rsi_pipeline, contrast, vision."
    domain: "earth.seismic"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_subsurface_model
    name: geox_subsurface_model
    description: "Subsurface model building: joint inversion, gravity/magnetic forward, MT forward."
    domain: "earth.model"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_basin
    name: geox_basin
    description: "Unified basin intelligence: profile, resolve, macrostrat, backstrip, mass balance, thermal maturity, map context, deep time, tectonic reconstruction."
    domain: "earth.basin"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_sequence
    name: geox_sequence
    description: "Unified stratigraphy: sequence analysis, biostratigraphic parsing and falsification. Modes: correlation, biostrat_parse, biostrat_falsify."
    domain: "earth.stratigraphy"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_geomechanics
    name: geox_geomechanics
    description: "Geomechanical analysis: bulk/shear/young modulus, poisson ratio, acoustic impedance, stress polygon."
    domain: "earth.structure"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_deep_time_state
    name: geox_deep_time_state
    description: "Earth State Vector through deep time: GPTS, CO2, sea level, temperature, paleogeography."
    domain: "earth.time"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_claim
    name: geox_claim
    description: "Unified claim lifecycle: create, validate, challenge, seal, evidence, consequence, optionality, cascade, feedback integrity, truth challenge."
    domain: "governance"
    lane: "judgment"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_prospect
    name: geox_prospect
    description: "Prospect evaluation: volumetrics, POS, EVOI, risk assessment. Modes: screen, evaluate."
    domain: "earth.prospect"
    lane: "judgment"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_gravmag_studio
    name: geox_gravmag_studio
    description: "Gravity/magnetic studio: forward modeling and screening. Modes: open, screen."
    domain: "earth.geophysics"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_well_desk
    name: geox_well_desk
    description: "Well desk: interactive view, publish rendered panel, render well panel. Modes: open, publish, render."
    domain: "earth.well"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_surface_status
    name: geox_surface_status
    description: "Federation-standard registry probe: tool discovery and health status."
    domain: "earth.meta"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_evidence
    name: geox_evidence
    description: "Unified evidence synthesis: discover, synthesize, abduct, contradict, spatial_block, ingest_literature."
    domain: "governance.evidence"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_basin_backstrip
    name: geox_basin_backstrip
    description: "1D basin backstripping: Steckler & Watts 1978 + Sclater & Christie 1980. Reconstruct tectonic and total subsidence through time."
    domain: "earth.basin"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_sediment_mass_balance
    name: geox_sediment_mass_balance
    description: "Source-to-sink sediment volume and mass accounting. Peters 2012 framework."
    domain: "earth.basin"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_thermal_maturity_history
    name: geox_thermal_maturity_history
    description: "Burial + heat flow + maturity modeling through time. EasyRo + TTI."
    domain: "earth.basin"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_claim_graph_evaluate
    name: geox_claim_graph_evaluate
    description: "Evaluate parent claim validity when child claims fail. DAG evaluation with AND/OR/WEIGHTED dependencies."
    domain: "governance"
    lane: "judgment"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_falsify
    name: geox_falsify
    description: "Popperian falsification engine. Modes: full (Kill Matrix K001-K007 + contradiction scan), quick (Kill Matrix only), physics_only (contradiction scan only), carbonate_igneous_rte (Arif Burgess-RTE Matr"
    domain: "governance"
    lane: "judgment"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_contradiction_scan
    name: geox_contradiction_scan
    description: "Scan claims for contradictions using13-type ontology. Classifies severity (FATAL/HIGH/MEDIUM/LOW) and resolution path (VOID/HOLD/DEMOTE/ESCALATE). FATAL →888_HOLD."
    domain: "governance"
    lane: "judgment"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_lem_predict
    name: geox_lem_predict
    description: "LEM inference — predict rock properties (porosity, Sw, lithology) from well log curves. Physics-prior mode (Archie, Gardner, Wyllie) until federated weights deploy."
    domain: "earth.inference"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_to_wealth_bridge
    name: geox_to_wealth_bridge
    description: "Cross-organ bridge: converts GEOX prospect economics to WEALTH score_kernel input. F2: epistemic_source preserved. F13: blocked nodes rejected."
    domain: "governance.bridge"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_map_export_package
    name: geox_map_export_package
    description: "Map export: package map layers and annotations into portable formats."
    domain: "earth.map"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_visual_understand
    name: geox_visual_understand
    description: "Extract visual patterns from seismic section images using deep learning."
    domain: "earth.seismic"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_visual_generate_hypotheses
    name: geox_visual_generate_hypotheses
    description: "Generate geological hypotheses across discontinuity gaps in seismic data."
    domain: "earth.seismic"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_map_layers_list
    name: geox_map_layers_list
    description: "List available map layers and their spatial extents."
    domain: "earth.map"
    lane: "discovery"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_map_scene_plan
    name: geox_map_scene_plan
    description: "Plan map scene composition: layer ordering, zoom, and annotation layout."
    domain: "earth.map"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_map_render_preview
    name: geox_map_render_preview
    description: "Render preview of map scene for quality review before export."
    domain: "earth.map"
    lane: "reasoning"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

  - id: geox_workspace
    name: geox_workspace
    description: "GEOX Workspace — persistent geological context across all tools. Set basin/play/well once; every tool inherits. Modes: set, view, history, evidence, relations, reset."
    domain: "earth.meta"
    lane: "evidence"
    input_schema:
      type: object
      properties: {}
      additionalProperties: true

# Constitutional governance
governance:
  floors:
    - { id: F1, name: Amanah, description: Reversibility }
    - { id: F2, name: Truth, description: All claims Earth-verified }
    - { id: F4, name: Clarity, description: Units and coordinates required }
    - { id: F7, name: Humility, description: Uncertainty calibrated }
    - { id: F9, name: Anti-Hantu, description: No hallucinated geology }
    - { id: F11, name: Authority, description: Provenance mandatory }
    - { id: F13, name: Sovereign, description: Human veto hook active }

# Verdict system
verdicts:
  SEAL:    { confidence_min: 0.80, action: auto_proceed }
  PARTIAL: { confidence_min: 0.50, action: proceed_with_caveats }
  SABAR:   { confidence_min: 0.25, action: hold_wait_data }
  VOID:    { confidence_min: 0.00, action: block_stop }

# Author
author:
  name: Muhammad Arif bin Fazil
  email: ariffazil@gmail.com
  org: Operator Exploration
  github: https://github.com/ariffazil

license: BSL-1.1
links:
  github: https://github.com/ariffazil/GEOX
  documentation: https://github.com/ariffazil/GEOX#readme

# Regenerated 2026-07-23 from CANONICAL_PUBLIC_SURFACE.json (schema geox.canonical_public_surface.v1)
# Advertised tool count: 31 = canonical public_count: 31
seal: DITEMPA BUKAN DIBERI
