# GEOX Earth Intelligence Tool Registry
# SOT: src/geox_mcp/registry.py — CANONICAL_PUBLIC_TOOLS
# Regenerated: 2026-07-16 (zen consolidation)
#
# DITEMPA BUKAN DIBERI — Forged, Not Given.
#
# THIS FILE IS GENERATED FROM registry.py. DO NOT EDIT MANUALLY.
# The single source of truth is src/geox_mcp/registry.py:CANONICAL_PUBLIC_TOOLS.
# If tools.yaml and registry.py disagree, registry.py wins.

schema_version: "2026.07.16"
organ: GEOX
authority: ariffazil/geox
mcp_transport: streamable-http
mcp_endpoints:
  direct: https://geox.arif-fazil.com/mcp
  kernel_gateway: https://mcp.arif-fazil.com/mcp
  local: http://127.0.0.1:8081/mcp
health_endpoint: http://127.0.0.1:8081/health

canonical_tool_count: 15
contract_epoch: "2026-07-16-GEOX-15TOOLS-ZEN"
source_of_truth: src/geox_mcp/registry.py::CANONICAL_PUBLIC_TOOLS
floor_enforcement: delegated-to-arifos
physics_boundary: Physics9

tools:
  # ── WELL DOMAIN ──────────────────────────────────────────────────────────────
  geox_well_ingest:
    category: earth.well
    lane: evidence
    risk_tier: readonly
    description: LAS, SEG-Y, DST, deviation, tops ingest

  geox_petrophysics:
    category: earth.petrophysics
    lane: reasoning
    risk_tier: readonly
    description: Vsh, porosity, Sw, perm, net pay, LEM, QC

  # ── SEISMIC DOMAIN ───────────────────────────────────────────────────────────
  geox_seismic_ingest:
    category: earth.seismic
    lane: evidence
    risk_tier: readonly
    description: SEG-Y I/O, header inspection, export

  geox_seismic_compute:
    category: earth.seismic
    lane: reasoning
    risk_tier: readonly
    description: Synthetic, well-tie, attribute, inversion, AVO

  geox_seismic_interpret:
    category: earth.seismic
    lane: reasoning
    risk_tier: readonly
    description: Horizon/fault picking, RSI pipeline, vision, contrast

  # ── MODEL DOMAIN ─────────────────────────────────────────────────────────────
  geox_subsurface_model:
    category: earth.model
    lane: reasoning
    risk_tier: c1
    description: Joint inversion, gravity/magnetic forward, MT forward

  # ── BASIN DOMAIN ─────────────────────────────────────────────────────────────
  geox_basin:
    category: earth.basin
    lane: reasoning
    risk_tier: readonly
    description: Profile, resolve, macrostrat, backstrip, mass balance, thermal

  geox_sequence:
    category: earth.stratigraphy
    lane: reasoning
    risk_tier: readonly
    description: Sequence analysis, biostratigraphic parsing and falsification

  # ── GEOMECHANICS ─────────────────────────────────────────────────────────────
  geox_geomechanics:
    category: earth.structure
    lane: reasoning
    risk_tier: readonly
    description: Bulk/shear/young modulus, poisson ratio, AI, stress polygon

  # ── DEEP TIME ────────────────────────────────────────────────────────────────
  geox_deep_time_state:
    category: earth.time
    lane: evidence
    risk_tier: readonly
    description: GPTS, CO2, sea level, temperature, paleogeography

  # ── GOVERNANCE ───────────────────────────────────────────────────────────────
  geox_claim:
    category: governance
    lane: judgment
    risk_tier: c2
    description: "Claim lifecycle: create, validate, challenge, seal, evidence"

  geox_prospect:
    category: earth.prospect
    lane: judgment
    risk_tier: c1
    description: Volumetrics, POS, EVOI, risk assessment

  # ── GEOPHYSICS ───────────────────────────────────────────────────────────────
  geox_gravmag_studio:
    category: earth.geophysics
    lane: reasoning
    risk_tier: readonly
    description: Gravity/magnetic forward modeling and screening

  # ── WELL DESK ────────────────────────────────────────────────────────────────
  geox_well_desk:
    category: earth.well
    lane: evidence
    risk_tier: readonly
    description: Interactive view, publish rendered panel, render well panel

  # ── META ─────────────────────────────────────────────────────────────────────
  geox_surface_status:
    category: earth.meta
    lane: discovery
    risk_tier: readonly
    description: "Federation-standard registry probe: tool discovery and health"
