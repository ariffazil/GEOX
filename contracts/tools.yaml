# GEOX Earth Intelligence Tool Registry
# SOT: src/geox_mcp/registry.py — CANONICAL_PUBLIC_TOOLS
# Regenerated: 2026-09-17T21:12:50.927635+00:00
#
# DITEMPA BUKAN DIBERI — Forged, Not Given.
#
# THIS FILE IS GENERATED FROM registry.py. DO NOT EDIT MANUALLY.
# The single source of truth is src/geox_mcp/registry.py:CANONICAL_PUBLIC_TOOLS.
# If tools.yaml and registry.py disagree, registry.py wins.

schema_version: 2026.09.17
organ: GEOX
authority: ariffazil/geox
mcp_transport: streamable-http
mcp_endpoints:
  direct: https://geox.arif-fazil.com/mcp
  kernel_gateway: https://mcp.arif-fazil.com/mcp
  local: http://127.0.0.1:8081/mcp
health_endpoint: http://127.0.0.1:8081/health
canonical_tool_count: 31
contract_epoch: 2026-09-17-GEOX-31TOOLS-ZEN
source_of_truth: src/geox_mcp/registry.py::CANONICAL_PUBLIC_TOOLS
floor_enforcement: delegated-to-arifos
physics_boundary: Physics9
tools:
  geox_basin:
    category: earth.basin
    lane: reasoning
    family: evidence
    capability_tier: A
    risk_tier: readonly
    description: 'Unified basin intelligence: profile, resolve, macrostrat, backstrip,
      mass balance, thermal maturity, map context, deep time, tectonic reconstruction.'
  geox_calibration_register_witness:
    category: earth.structural
    lane: compute
    family: structural
    capability_tier: B
    risk_tier: readonly
    description: Register a CalibrationWitness for structural interpretation. Validates
      schema, checks completeness blockers (missing axes, depth-without-velocity).
      Returns VOID on bad input, HOLD on missing calibration.
  geox_claim:
    category: earth.governance
    lane: reasoning
    family: evidence
    capability_tier: A
    risk_tier: readonly
    description: 'Claim lifecycle: create, validate, challenge, seal, evidence, falsify,
      synthesize, contradict. Modes: create, validate, challenge, seal, attach, falsify,
      discover, synthesize, abduct, contradict, scan.'
  geox_contrast_metabolize:
    category: earth.seismic
    lane: reasoning
    family: interpret
    capability_tier: B
    risk_tier: readonly
    description: 'Unified anomalous contrast metabolic pipeline. Binds three stages
      into one call: ISOLATE (deterministic acoustic impedance contrast detection)
      then MEASURE (AVO gradient + LMR estimates) then CLASSIFY (>=3 stratigraphic
      trap hypotheses). substrate_class=INERT. Local max QUALIFIED_CANDIDATE - arifOS
      seals.'
  geox_deep_time:
    category: earth.time
    lane: compute
    family: evidence
    capability_tier: A
    risk_tier: readonly
    description: 'Deep time: Earth state, dynamical systems, DDE reasoning. Modes:
      dde_reason.'
  geox_extract_display_proxy:
    category: earth.seismic
    lane: compute
    family: seismic
    capability_tier: B
    risk_tier: readonly
    description: Extract display-derived proxy from raster seismic image. Requires
      CalibrationWitness. Output labeled DISPLAY_DERIVED_PROXY — NOT suitable for
      AVO, amplitude preservation, or native well-tie.
  geox_extract_native_trace:
    category: earth.seismic
    lane: compute
    family: seismic
    capability_tier: B
    risk_tier: readonly
    description: Extract a native seismic trace from a registered source. Returns
      NativeTraceRef with full provenance. HOLD for unverified authority, restricted
      data, or missing source.
  geox_geomechanics:
    category: earth.structure
    lane: reasoning
    family: interpret
    capability_tier: B
    risk_tier: readonly
    description: 'Geomechanical analysis: bulk/shear/young modulus, poisson ratio,
      acoustic impedance, stress polygon.'
  geox_glof_cascade_initialize:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: GLOF cascade simulation init. Accepts 33D GEOXEarthState33 (M9+P9+W9+X6
      zen-33) or defaults to himalayan_glof_33. Returns session state_id.
  geox_glof_cascade_inverse:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: Bayesian grid-search inference on 33D posterior from wave observations
      (W obs). Returns inferred theta_hat + log_likelihood + posterior_summary.
  geox_glof_cascade_mcmc_inverse:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: Phase C — proper Bayesian posterior via adaptive Metropolis-Hastings
      MCMC. Replaces grid search. Returns posterior mean, std, percentiles, R-hat,
      ESS, accept_rate, n_samples. Sampling on (E, c, phi, tau_0, sigma_t, phi_p).
  geox_glof_cascade_metabolize:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: Close F-I-M loop on 33D state. Emit FIMReceipt33 with tri-witness
      G-score, phase transitions, cycle_id.
  geox_glof_cascade_phase:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: Yield surface eval on 33D state. Returns Mohr-Coulomb/Voellmy verdict
      + 33D-derived sigma_eff, tau_max, breach_probability_33D, hydro_fracture indicator.
  geox_glof_cascade_propagate:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: Phase C — 1D Saint-Venant (shallow-water) wave propagation from breach
      to downstream. Lax-Friedrichs finite-volume solver with Manning friction. Returns
      time series of (h, u, Q) along domain + peak downstream Q/time/depth/velocity.
  geox_glof_cascade_step:
    category: earth.hazard
    lane: reasoning
    family: research
    capability_tier: C
    risk_tier: readonly
    description: Advance 33D GLOF state by n_steps. Uses metabolize_33 closure + forward_kinematics
      to derive W (Vp,Vs,alpha_B). Returns evolved 33D state.
  geox_list_registered_sources:
    category: earth.seismic
    lane: compute
    family: seismic
    capability_tier: B
    risk_tier: readonly
    description: List all registered native seismic sources with classification, authority,
      trace count, and sample metadata.
  geox_map:
    category: earth.spatial
    lane: compute
    family: view
    capability_tier: A
    risk_tier: readonly
    description: 'Map intelligence: layer discovery, scene composition, preview. Modes:
      layers_list, scene_plan, render_preview.'
  geox_model:
    category: earth.model
    lane: compute
    family: model
    capability_tier: A
    risk_tier: readonly
    description: 'Geological modeling: subsurface inversion, 2D cross-sections, GemPy
      3D. Modes: subsurface, geological_generate, gempy_3d.'
  geox_paleobiodb_query:
    category: earth.paleobiology
    lane: evidence
    family: evidence
    capability_tier: B
    risk_tier: readonly
    description: 'Query the Paleobiology Database (PBDB v1.2) for taxa, occurrences,
      and biozones. Modes: taxa (resolve a taxon), occurrence (fossil occurrence records),
      zone (biozones by scale), age_intervals (ICS / nanno / foram zones). Read-only,
      public-domain fossil data.'
  geox_petrophysics:
    category: earth.petrophysics
    lane: reasoning
    family: interpret
    capability_tier: A
    risk_tier: readonly
    description: 'Unified petrophysics: Vsh, porosity, Sw, permeability, net pay,
      LEM inference, QC. Modes: generate, verify, lem_inference, stoip_feed, qc.'
  geox_prospect:
    category: earth.prospect
    lane: judgment
    family: prospect
    capability_tier: A
    risk_tier: readonly
    description: 'Prospect evaluation: volumetrics, POS, EVOI, risk assessment. Modes:
      screen, evaluate.'
  geox_register_native_source:
    category: earth.seismic
    lane: compute
    family: seismic
    capability_tier: B
    risk_tier: readonly
    description: 'Register a native SEG-Y source for trace extraction. Validates file
      existence, computes SHA256, extracts binary header metadata. Authority downgrade:
      OPERATOR_APPROVED without actor → UNVERIFIED.'
  geox_seismic_compute:
    category: earth.seismic
    lane: reasoning
    family: interpret
    capability_tier: B
    risk_tier: readonly
    description: 'Seismic computation: forward model, well tie, attributes, wavelet
      extraction, inversion.'
  geox_seismic_ingest:
    category: earth.seismic
    lane: evidence
    family: ingest
    capability_tier: B
    risk_tier: readonly
    description: SEG-Y I/O, header inspection, export.
  geox_seismic_interpret:
    category: earth.seismic
    lane: reasoning
    family: interpret
    capability_tier: A
    risk_tier: readonly
    description: 'One semantic capability — propose geometry, physics-gate, compare
      hypotheses (NOT autonomous SEAL). Live modes: horizon_contrast, fault_sticks,
      volume_frame, blend, structure_validate, interpret (interpretation_bundle ≥3
      hyps), interpret_section/rsi_pipeline (INT_SEISMIC), segy_slice. Gates return
      PASS|WARN|KILL|UNMEASURED + receipt_hash. preferred_hypothesis always null from
      GEOX. Local max QUALIFIED_CANDIDATE — arifOS seals only.'
  geox_source:
    category: earth.source
    lane: compute
    family: evidence
    capability_tier: A
    risk_tier: readonly
    description: 'Source rock & diagenesis: TOC, kerogen, maturity, compaction. Modes:
      source_rock, diagenesis.'
  geox_spatial:
    category: earth.spatial
    lane: compute
    family: evidence
    capability_tier: A
    risk_tier: readonly
    description: 'Spatial intelligence: H3, LanceDB, STAC. Modes: h3_index, lancedb_store,
      stac_discover.'
  geox_temporal:
    category: earth.temporal
    lane: compute
    family: evidence
    capability_tier: A
    risk_tier: readonly
    description: 'Temporal analytics: decline curve, RRR, basin lifecycle, cadence.
      Modes: decline, rrr, basin_lifecycle, cadence.'
  geox_well:
    category: earth.well
    lane: reasoning
    family: interpret
    capability_tier: A
    risk_tier: readonly
    description: 'Well intelligence: view curves, open desk, desurvey. Modes: view,
      desk.'
  geox_well_ingest:
    category: earth.well
    lane: evidence
    family: ingest
    capability_tier: B
    risk_tier: readonly
    description: Load well log data from LAS, SEG-Y, DST, deviation, or tops files.
      Auto-detects format.
  geox_well_qc:
    category: earth.well
    lane: evidence
    family: ingest
    capability_tier: A
    risk_tier: readonly
    description: 'Quality control for well data: depth monotonicity, null %, physical
      range checks.'
