# GEOX MCP — Minimal Production Stack
# DITEMPA BUKAN DIBERI
# Arif Spec: Low-entropy, physics-grounded, no bloatware

# Core Computational & Spatial Physics
numpy>=2.4.6
scipy>=1.17.1
pandas>=2.0.0

# Visual Abstraction & Imaging Membrane (headless = no GUI bloat)
opencv-python-headless>=4.7.0
matplotlib>=3.7.0

# MCP Framework
fastmcp>=3.2.4,<4.0
pydantic>=2.0.0
uvicorn>=0.23.0

# Data Ingestion
lasio>=0.32
openpyxl>=3.1.5

# HTTP & async
httpx>=0.25.0
python-dotenv>=1.0.0

# Database
asyncpg>=0.29.0

# LLM Integration (LEM Layer)
google-genai>=0.1.0

# Production Seismic I/O (SEGY)
segyio>=1.9.0

# Earth Science Stack (trimmed — production only)
-r requirements-earth.txt
