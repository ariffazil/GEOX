# 🌍 GEOX — Federation Documentation

> **Role:** DOMAIN L3 — Earth Intelligence / Governed World Model
> **Citizenship:** warga-aaa | **Port:** 8081 | **Doctrine:** DITEMPA BUKAN DIBERI

## Federation Position

```
arifOS (Ω Law, :8088) → GEOX (Earth Evidence, :8081) → arifOS 888_JUDGE → A-FORGE (Execution, :7071)
```

GEOX is the earth evidence coprocessor. It computes geoscience, petrophysics, and physics-grounded evidence for constitutional judgment. It NEVER adjudicates, NEVER executes.

## Cross-Organ Links

| Organ | Relationship | Contract |
|-------|-------------|----------|
| **arifOS** (:8088) | Receives GEOX evidence for constitutional judgment; GEOX calls `arif_judge` for SEAL/HOLD/SABAR/VOID on prospect evaluations | kernel governance gates |
| **AAA** (:3001) | Cockpit display of GEOX maps, well data, basin synthesis; A2A routing for cross-organ queries | cockpit + A2A gateway |
| **A-FORGE** (:7071) | Deploys GEOX MCP server, runs forge_* lifecycle; GEOX never self-deploys | execution shell |
| **WEALTH** (:18082) | Receives GEOX prospect economics via `geox_to_wealth_bridge` for capital intelligence | `GEOC_to_WEALTH_bridge` |
| **WELL** (:18083) | Receives GEOX substrate intelligence for vitality mirroring | substrate vitality |
| **VAULT999** | All GEOX SEAL-grade evidence receipts sealed to immutable ledger | immutable audit chain |

## ATLAS333 Paradox Map

GEOX GENESIS documents map to ATLAS333 cognitive geometry paradoxes:

| Paradox | GEOX Coverage |
|---------|---------------|
| P3 (Truth/Uncertainty) | `000_MANIFESTO` · `012_GEOX_001` |
| P4 (Evidence/Claim) | `001_KILL_MAP` · `003_CONSTITUTIONAL_ALIGNMENT` · `013_METABOLIC_SURFACE` |
| P12 (Capability/Authority) | `004_BOUNDARIES` · `010_AEI_GOVERNANCE` · `016_CONSTITUTIONAL_PROMPT` |
| P14 (Reason/Intuition) | `002_FIRST_PRINCIPLES` · `014_PETREL_DSG_DELTA` |
| P15 (Local/Global) | `005_AGI_LOOP_RESEARCH` · `006_AGI_LOOP` · `011_COMPETITIVE_LAYER_MAP` · `015_AGENTIC_GEOLOGY` |
| P18 (Observer/Observed) | `003a_PHYSICS_MANIFEST` · `007_GEOX_CHARTER` |
| P21 (Measurable/Meaningful) | `008_FULL_EARTH_MODEL_LOOP` |
| P29 (Sovereignty) | `009_EARTH_ORGAN_CONTRACT` · `017_EARTHOS_CONSTITUTION` |

Full ATLAS333 cross-repo map: `/root/A-FORGE/forge_work/2026-07-20/ATLAS333-CROSS-REPO-MAP.md`
GENESIS index: `/root/GEOX/GENESIS/README.md`

## Canonical Federation Contract

See `./FEDERATION_CONTRACT.md` (thin pointer) → `/root/arifOS/FEDERATION_CONTRACT.md` (canonical source).

## Two-Entry-Point Doctrine

GEOX has two MCP entry points per the Two-Entry-Point Doctrine in `FEDERATION_CONTRACT.md`:

| Endpoint | Authority Model | Use Case |
|----------|----------------|----------|
| `mcp.arif-fazil.com/mcp` | Governance-first (envelope-wrapped via arifOS) | Judgment tools, session binding, seal |
| `geox.arif-fazil.com/mcp` | Lane-gated (direct) | Discovery, evidence, reasoning |

Discovery and evidence lanes MAY use direct path. Judgment lane MUST route through kernel.

---

**DITEMPA BUKAN DIBERI — Forged, Not Given.**
**Part of the arifOS Federation.**
