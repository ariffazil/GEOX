# GEOX × Δ·Ω·Ψ × Multimodal × G-Compass Map

> **Status:** ACTIVE · next organ wiring after doctrine seal  
> **Forged:** 2026-07-25  
> **Session:** post `SEAL-107656cfb71d4558`  
> **Doctrine parents:**  
> - `/root/AAA/governance/MULTIMODAL_AGI_DOCTRINE.md`  
> - `/root/AAA/governance/G_FOLD_AS_COMPASS.md`  
> - `/root/GEOX/src/geox_core/io/OFFLINE_STUBS.md`  
> **Domain law:** NATURAL_LAW (Earth truth) — evidence organ, not court  
> **DITEMPA BUKAN DIBERI**

---

## 0. GEOX role in the stack

```
LLM / tools  →  multimodal witness instruments
GEOX         →  Ext_witness (Earth) — NATURAL_LAW evidence
arifOS Δ     →  metabolize + mint G (apex)
arifOS 888   →  judge
A-FORGE Ψ    →  act after SEAL
VAULT999     →  record
```

**GEOX is not the judge.**  
**GEOX is the External witness organ for Earth multimodal evidence.**

---

## 1. Plane map (GEOX-specific)

| Plane | GEOX duty | Preferred home |
|-------|-----------|----------------|
| **Δ** | Metabolize seismic, wells, logs, maps into claims + QC + gates | Python `geox_mcp` / `geox_core` |
| **Ω** | MCP tool schemas, health envelopes, cockpit panels | TS panels / JSON contracts — shape only |
| **Ψ** | Never self-SEAL Earth truth; hand SEAL-grade claims to kernel | A-FORGE only after `arif_judge` |
| **Φ Ext** | External witness leg of tri-witness | Live measurements, not offline stubs |
| **G** | **Read** kernel apex_scalars; never mint constitutional G | `arif_think(mode=apex)` only |

---

## 2. Multimodal Earth paths (Δ metabolism)

| Modality | GEOX surface | Metabolism rule |
|----------|--------------|-----------------|
| Seismic images / sections | seismic vision, classical_section, interpret bundles | Claims + geometry gates + witness_id |
| Well logs / LAS | petrophysics tools | Curves → features → OBS/DER tags |
| Maps / WMS / grids | io fetchers | mode=live \| offline_stub must be explicit |
| Structure | structure_validate, K-DIP/K-THROW | Physics9 bounds; HOLD impossible structure |
| Documents / PDFs | document pipelines if present | Provenance + extract |

**Rule:** Raw pixels/arrays are not truth. Only **metabolized evidence** enters Φ Ext.

---

## 3. G-fold flow (GEOX)

| Step | Behavior |
|------|----------|
| 1 | Prefer session apex from `arif_think(mode=apex)` when agent has SCT |
| 2 | Health may **echo** kernel `/health` apex_scalars if status is MEASURED |
| 3 | Reject **NOMINAL** / stub vitals (fabricated 0.5) → UNMEASURED |
| 4 | Domain scores (beauty, phi, ranking) **never rename to G** |
| 5 | Offline_stub Ext → Ext_witness weak/zero for SEAL geometry |

**Live fix (this session):** health handler hardened to drop NOMINAL; restart geox-mcp.

---

## 4. Offline stubs (H7) as Ext_witness poison

18 fetchers default offline → `mode=offline_stub`.

| Agent rule | |
|------------|--|
| Always surface `mode` in evidence | F2 |
| `require_live=1` paths must fail closed on stub | next engineering |
| Never promote stub to Ext_witness for Φ | G-compass |
| Inventory | `src/geox_core/io/OFFLINE_STUBS.md` |

---

## 5. Protocol: GEOX multimodal turn

```
1. Ingest via GEOX tool (seismic/well/map/structure)
2. Attach provenance: organ=geox, tool, timestamp, mode=live|offline_stub
3. Metabolize: claims, gates (PASS/WARN/KILL/UNMEASURED), witnesses[]
4. Do NOT compute constitutional G inside GEOX
5. If seal-grade interpretation:
     arif_think(mode=apex) with Ext from GEOX + AI + H
     arif_judge → only then seal narrative
6. Receipt: earth evidence hash + g_authority + verdict
```

---

## 6. Engineering backlog (ordered)

| P | Work | Risk |
|---|------|------|
| **P0** | Health: never emit NOMINAL G (this commit + restart) | Low |
| **P1** | Tool results: stamp `ext_witness_ready: bool` + `mode` | Low |
| **P1** | Smoke: fail if offline_stub under `GEOX_REQUIRE_LIVE=1` | Low |
| **P2** | Bridge: session apex_scalars on interpret receipts | Med |
| **P2** | Drift: tools_loaded 33 vs live 73 surface_drift | Med |
| **P3** | Archive cold fetchers after import-graph proof | Med |

---

## 7. Success metrics (GEOX)

| Metric | Target |
|--------|--------|
| Health apex status NOMINAL | **0** |
| Health apex from kernel MEASURED/UNMEASURED only | **100%** |
| Interpret outputs without `mode` | **0** |
| Constitutional G computed inside GEOX | **0** |
| Stub used as Ext for SEAL without flag | **0** |

---

## 8. One line

> GEOX sees the Earth. Δ metabolizes it. Φ counts it as Ext. G is still minted in arifOS. GEOX never wears the crown.

**DITEMPA BUKAN DIBERI**
