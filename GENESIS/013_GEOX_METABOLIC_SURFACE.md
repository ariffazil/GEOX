# GENESIS/013 — GEOX Metabolic Surface Map

> Orthogonal Base → Domain Lenses → Dimensions/Fractals → Cognitive → Law & Evidence  
> **Routing law for GEOX-001:** Orthogonal Base first. No Cognitive / Dimensional tools until ingest + QC + tie custody exists.

---

## Constitutional geometry

| Plane | Role | Floor stress |
|-------|------|--------------|
| **1. Orthogonal Base** | Raw physical primitives + calibration custody | F2 TRUTH |
| **2. Domain Lenses** | Discipline extractors (time, rock, basin) | F2 + F7 |
| **3. Dimensions & Fractals** | Spatial / temporal / forward simulation | F4 + F9 |
| **4. Cognitive synthesizers** | Vision / RSI / hypothesis rank | F9 ANTI-HANTU |
| **5. Law & Evidence** | Claims, EGS, contrast, wealth bridge | F11 + F13 |

---

## 1. Orthogonal Base (Physical Primitives & Ingest)

### Well pipeline (1D)
- `geox_well_ingest`
- `geox_well_qc`
- `geox_well_desurvey`

### Seismic pipeline (3D)
- `geox_seismic_ingest`
- `geox_segy_audit`
- `geox_segy_trace_audit`
- `geox_seismic_compute` / EGS seismic compute

### Calibration bridge (1D ↔ 3D)
- `geox_tie_preflight`
- `geox_well_tie_compute`
- `geox_well_tie`
- `geox_tie_receipt`

**GEOX-001 spine (mandatory order):**

```text
geox_well_ingest(mode=las|checkshot|tops)
  → geox_well_qc
  → [geox_seismic_ingest | segy_audit]  # when SEG-Y present
  → geox_tie_preflight
  → geox_well_tie_compute | geox_well_tie | synthetic tie engine
  → geox_tie_receipt
  → ONLY THEN: geox_claim / geox_contrast_detect / geox_benchmark_001 verdict
```

---

## 2. Domain Lenses

**Time:** `geox_deep_time_state`, `geox_macrostrat_calibrate`, `geox_biostrat_parse`, `geox_biostrat_nn_age`, `geox_biostrat_falsify`, `geox_biostrat_ruling_check`  
**Physics:** `geox_petrophysics`, EGS rock physics, `geox_geomechanics`  
**Basin/prospect:** `geox_basin`, `geox_prospect`

---

## 3. Dimensions & Fractals

**Maps:** `geox_atlas`, `geox_map_layers_list`, `geox_map_scene_plan`, `geox_map_render_preview`, `geox_map_export_package`  
**Simulation:** `geox_sequence`, `geox_simulate_sequences`, `geox_simulate_accommodation`, `geox_simulate_routing`, `geox_simulate_surfaces`, `geox_surface_status`  
**3D:** `geox_subsurface_model`, `geox_3d_model_build`, `geox_3d_model`

**Forbidden before Orthogonal Base SEAL-grade custody:** simulate_*, map_*, 3d_model_* on GEOX-001 path.

---

## 4. Cognitive & Visual Synthesizers

`geox_vision`, visual enhance/understand/hypotheses, `geox_physical_reality_interpret`, `geox_seismic_interpret`, RSI, geological cognition, cognitive rank.

**Forbidden on GEOX-001 until tie receipt exists** — interpretation is not calibration.

---

## 5. Law & Evidence Plane

**EGS / claims:** `geox_claim`, `geox_evidence`, EGS claim/evidence/query tools, `geox_forbidden_claims_scan`  
**Audit:** render audit, panel D  
**Judgment & value:** `geox_contrast_detect`, **`geox_benchmark_001`**, wealth bridge/consequence

Claims after tie. Contrast after evidence. Wealth after earth.

---

## Iron rule (ΔS ≤ 0)

```text
IF orthogonal_base_incomplete:
    VOID cognitive_and_dimensional_promotion
    HOLD any claim that pretends horizon = reservoir without tie receipt
```

This is the migration proof: not a 7-tool coprocessor — a layered evidence organ.  
GEOX-001 proves the organ only when it **refuses to skip the base**.

---

*GENESIS 013 · 2026-07-09 · pairs with 012 GEOX-001*  
*DITEMPA BUKAN DIBERI*
