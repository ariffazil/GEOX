# GEOX Earth Science Stack — TRIMMED
# DITEMPA BUKAN DIBERI
# Kept: only packages imported in canonical src/ OR in Arif's wajib spec
# Removed: bloatware (panel, bokeh, holoviews, gempy, subsurface, etc.)
# Audit: zero imports found in canonical src/ for removed packages

# Core Scientific (Wajib per Arif spec)
numpy>=2.4.6
scipy>=1.10.0
pandas>=2.0.0,<3.0.0  # pinned <3 until pandas 3.x is stable (dependabot PR #67 closed wontfix)
matplotlib>=3.7.0

# Imaging support (Pillow — matplotlib/OpenCV dependency)
pillow>=10.0.0

# Seismic I/O (Wajib per Arif spec — segyio production-ready, segysak for SEG-Y)
segyio>=1.9.0
segysak>=0.5.4

# Well Log Ingestion (canonical src/ uses lasio)
welly>=0.5.2
lasio>=0.31
striplog>=0.9.2

# Mesh I/O (used by structural geology output)
trimesh>=4.12.2
meshio>=5.3.5

# ML / Stats (canonical src/ uses openpyxl)
scikit-learn>=1.3.0
statsmodels>=0.14.6
openpyxl>=3.1.5
